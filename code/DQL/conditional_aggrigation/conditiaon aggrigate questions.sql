-- Practice Questions
-- Beginner Level (10 Questions)
-- Q1. Count how many users are active and how many are inactive (no GROUP BY, just two numbers).
--
-- Q2. Show each country with total users and how many users are active.
--
-- Q3. Show each country with total users and how many users are inactive.
--
-- Q4. Count total orders per category and how many orders have price above 100.
--
-- Q5. Show total orders and total orders placed in January 2026.
--
-- Q6. Show each category with total orders and total orders where quantity = 1.
--
-- Q7. Count how many users are aged 30 or above vs below 30 (no GROUP BY, just two numbers).
--
-- Q8. Show each country with total users and users aged 30 or above.
--
-- Q9. Show total orders and total orders placed in the first quarter (Jan, Feb, Mar 2026).
--
-- Q10. Show each category with total revenue and revenue from orders where quantity > 1.
--------------------------------------------------------------------------------------------------------------------------------


-- Intermediate Level (10 Questions)
-- Q1. Show each country with: total users, active users, inactive users, and users aged below 30.
--
-- Q2. Show each category with: total orders, orders in Jan, orders in Feb, orders in Mar.
--
-- Q3. Show each user_id with: total orders, orders in Electronics, orders in Furniture, orders in Books.
--
-- Q4. Show each country with: total users, active seniors (active AND age >= 30), inactive juniors (inactive AND age < 30).
--
-- Q5. Show each category with: total revenue, revenue from quantity = 1 orders, revenue from quantity > 1 orders.
--
-- Q6. Show each user_id with: total amount spent, amount spent in January 2026, amount spent in February 2026.
--
-- Q7. Show each category with: total orders, orders above average price of that category, orders below average price.
--
-- Q8. Show each country with: total users, users with age between 25 and 35, users outside that range.
--
-- Q9. Show each user_id with: total orders, expensive orders (price > 300), cheap orders (price <= 300).
--
-- Q10. Show each category with: total revenue, revenue from active users only (JOIN with users table needed).
--------------------------------------------------------------------------------------------------------------------------------



-- Advanced Level (10 Questions)
-- Q1. Show each country with: total users, active users, and percentage of active users rounded to 2 decimal places.
--
-- Q2. Show each category with revenue for each month. Also add a column showing which month had the highest revenue (use CASE WHEN on the three month values).
--
-- Q3. Show each user_id with: total spent, amount spent on Electronics, Electronics spend as a percentage of total spent.
--
-- Q4. Show each country with: active users, average age of active users, average age of inactive users. Label the country "Healthy" if active users > inactive users, else "Unhealthy".
--
-- Q5. Show each category with: total orders, orders placed by active users, orders placed by inactive users. Include the JOIN with users.
--
-- Q6. Show each user_id with: total orders, orders above the overall average price, orders below the overall average price (subquery needed for average).
--
-- Q7. Show each country with: total revenue generated by its users (via orders JOIN), revenue in Q1 (Jan-Mar), and Q1 revenue as a percentage of total.
--
-- Q8. Show each category with: total revenue, average price, revenue from orders above average price, revenue from orders below average price.
--
-- Q9. Show each user_id with: total orders, consecutive months active (Jan orders > 0 AND Feb orders > 0 using CASE WHEN COUNT inside HAVING).
--
-- Q10. Show a full user report: country, total users, active users, inactive users, avg age of all users, avg age of active users, avg age of inactive users, and a label — "Top Country" if active users >= 2, else "Low Activity".
--------------------------------------------------------------------------------------------------------------------------------



--
-- Expert Level (10 Questions)
-- Q1. Show each category with revenue split by country (Electronics India revenue, Electronics USA revenue, Electronics UK revenue — all in one row per category). JOIN needed.
--
-- Q2. Show each user_id with: total orders, first order month label (Jan/Feb/Mar using CASE WHEN on MIN(order_date)), last order month label.
--
-- Q3. For each category, show total revenue, revenue rank label ("High" if > 1000, "Medium" if 500-1000, "Low" if < 500) using CASE WHEN on SUM.
--
-- Q4. Show each country: total users, active users, percentage active, average order value of users from that country, and label "Profitable" if avg order value > 300 else "Below Average".
--
-- Q5. Show each user_id with: total spent, total spent on orders above average price, total spent on orders below average price, and which category they spent the most on.
--
-- Q6. Build a pivot table: rows = category, columns = Jan revenue / Feb revenue / Mar revenue / Total revenue / Best Month label.
--
-- Q7. Show each country with: total users, users who ordered in Jan, users who ordered in Feb, users who ordered in both Jan and Feb.
--
-- Q8. Show each user_id with: total orders, total revenue, revenue as a percentage of overall total revenue across all users (subquery needed).
--
-- Q9. Show each category with: total orders, orders from users aged below 30, orders from users aged 30 and above, revenue from below-30 users, revenue from 30+ users, and which age group spends more as a label.
--
-- Q10. Build a complete summary: country, total users, active users, total orders placed by country users, total revenue from country users, average spend per user, and a performance label ("Strong" if avg spend > 400, "Moderate" if 200-400, "Weak" if below 200). Requires JOIN, GROUP BY, conditional aggregation, and CASE WHEN on SUM all in one query.